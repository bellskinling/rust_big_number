extern crate big_number;
use big_number::file_operation;
use big_number::big_number_core::*;
use std::fs::OpenOptions;
use std::io::Read;

fn main() {
    //let b = BigNumber::new("-1", 0);
    
    //println!("{}", b.to_string());
}
